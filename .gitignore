# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Results
results/
!docs/static/images/results/
outputs/
# Local config (머신별 설정 — git pull 시 보존됨)
.env
openai_api_key.json
google_aistudio_key.json

# Workspace-specific configs & calibrations (machine/robot별 관리)
# pipeline_config — 공개 yaml 은 tracked. secret/이미지 류만 ignore.
# (옛날엔 디렉토리 통째 무시 → phase2_config.yaml 등 원격 sync 안 되어 server
#  boot fail. 2026-05-21 회복.)
pipeline_config/paid_api_config.yaml
pipeline_config/free_api_config.yaml
pipeline_config/cad_images/
pipeline_config/side_view_images/
robot_configs/

# Legacy folders (DO NOT MODIFY)
code_gen/

# Claude Code local config
CLAUDE.md
claude_document/branch_guidelines.md

# Cloud inference (local development)
cloud_inference2/
# lerobot/ — 프로젝트에 포함하여 배포

# Local documents
참고자료_Docker_File_제출_가이드라인_및_목록_예시.md
RAPIDS_결과보고서_v3_3_1_1.md

# Model checkpoints and artifacts
models/checkpoints/
models/**/*.pt
models/**/*.pth

# mplib auto-generated SRDF (first-run, ~3min, derived from URDF)
*_mplib.srdf

# Curobo source (installed via pip -e from github, not part of repo)
src/nvidia-curobo/

# Editor / IDE backup files
*.bak_*

# user-side archives
*.zip

# Server-side vector DB cache (per-machine binary, regen via rebuild_p_phase1)
grpc_server/buffer/

# docs trajectory/dataset npz cache (downloaded, regenerable)
docs/.cache_*/
