# Charuco Board 기반 카메라-로봇 캘리브레이션

고정된 Top-view 카메라 좌표계와 고정된 로봇 베이스 좌표계 사이의 **변환행렬**을 구하기 위한 캘리브레이션 가이드.

---

## 1. 시나리오 정의

- **카메라**: 고정 (Top-view, RealSense D435 — color + depth)
- **로봇 베이스**: 고정
- **Charuco 보드**: 이동 가능 (사람이 손으로 옮김)
- **목표**: 카메라 좌표계 → 로봇 좌표계 **변환행렬** (4×4) 1개를 정밀하게 구하기

> 카메라도 로봇도 안 움직이므로 두 좌표계 사이는 **하나의 고정 변환행렬**.
> 따라서 이 문제는 **Hand-Eye AX=XB 가 아님**. 단순한 **3D-3D rigid 정렬** 문제.

---

## 2. 기존 방식(pix2robot 호모그래피)의 한계

현재 `pix2robot_calibrator`는 다음과 같이 동작:

```python
robot_xy = H @ [u, v, 1]            # 호모그래피로 (x, y) — z=table 평면 가정
z = table_depth - depth(u, v)        # z만 따로 depth로 갱신
```

| 한계 | 영향 |
|---|---|
| 평면 가정 (z=0)으로 (x,y) 결정 | 공중에 뜬 물체에서 **시차(parallax) 오차** |
| 렌즈 왜곡 미보정 | 이미지 가장자리에서 오차 누적 |
| 사람이 픽셀을 클릭 (±2px) | 캘리브 점 정확도 한계 |
| 점 13쌍 (수동 수집) | 분포가 좁으면 외삽 오차 |

특히 **블록 위에 블록을 쌓는 작업**처럼 z가 5~8cm로 커지면, 카메라 광축에서 멀어진 픽셀일수록 (x, y)가 다음만큼 어긋남:

```
Δxy ≈ object_height × tan(view_angle_from_nadir)
```

| 카메라 기울기 | 물체 높이 5cm | 물체 높이 10cm |
|---|---|---|
| 0° (완전 수직) | 0 mm | 0 mm |
| 15° | 13 mm | 27 mm |
| 30° | 29 mm | 58 mm |

### 시차(parallax) 오차란?

```
       Top-view 카메라 (광축이 약간 기울어짐)
            \
             \
              \  ← 픽셀 (u,v)의 광선
               \
                \
                 ●  ← 광선이 z=5cm와 만나는 진짜 물체 (x_real, y_real, 0.05)
                  \
                   \
                    ●  ← 광선이 z=0과 만나는 점 (x_table, y_table, 0)
       ─────────────── table (z=0)

   호모그래피의 답: (x_table, y_table)   ← z=0 가정으로 결정
   실제 물체 위치:  (x_real,  y_real)    ← 다름 → 시차 오차
```

**문제의 본질**: 호모그래피 + depth 방식은 **x, y는 평면 가정(z=0), z만 depth로 갱신** 하는 짬뽕 구조.

```python
robot_xy = H @ [u, v, 1]              # z=0 가정으로 (x,y) 결정
z = table_depth - depth(u,v)           # z만 갱신, x/y는 그대로
```

→ 같은 픽셀이 가리키는 실제 3D 점은 z에 따라 (x, y)가 달라지는데, 호모그래피는 그걸 모름.
→ **물체가 테이블 표면에 있을 때만 정확**, 공중에 뜬 물체(스택 윗면 등)는 광축에서 멀수록 어긋남이 z에 비례해 커짐.

---

## 3. Charuco 보드의 구성 요소

### 3.1 Charuco 코너 ≠ ArUco 마커 코너

| 종류 | 위치 | 캘리브에 사용 |
|---|---|---|
| ArUco 마커 코너 | 검은 사각형 마커의 4 모서리 | 보드 식별/지향 용 |
| **Charuco 코너** | **흰-검 사각형이 만나는 체스판 교차점** | ✅ 이걸 EE로 터치 |

각 Charuco 코너는:
- **고유 ID**가 자동 부여됨 (0, 1, 2, ...)
- 보드 좌표계에서 **위치가 정확히 알려짐**: 예) 25mm 사각형, 5×7 보드의 코너 ID 5 = `(0.050, 0.025, 0) m`

```
+---+---+---+---+
| ▣ |   | ▣ |   |
+---0---1---2---+   ← 0, 1, 2 ... = Charuco corner ID
|   | ▣ |   | ▣ |
+---3---4---5---+
| ▣ |   | ▣ |   |
+---+---+---+---+
```

### 3.2 Charuco가 주는 3가지 능력

```
Charuco 보드 1장 → 검출 1번
  ├─ ① 서브픽셀 코너 검출    (사람 클릭 ±2px → 알고리즘 ±0.05px)
  ├─ ② 코너 ID 자동 매칭      (어느 코너인지 자동 식별)
  └─ ③ 카메라 내부 파라미터   (K, dist) — 여러 자세 모아 추정
       └─ ④ 보드 자세 PnP    (카메라→보드 변환행렬)
```

### 3.3 ArUco "신뢰도"의 의미

ArUco 마커는 내부 비트 패턴이 **이진 코드**로 인코딩되어 있어:

1. **자기 검증**: 사전(dictionary)에 없는 패턴이면 즉시 기각 → False positive 거의 없음
2. **오류 정정**: Hamming 거리 기반 사전 → 비트 1~2개 오류 자동 복원
3. **회전 모호성 해결**: 비트 패턴이 비대칭 → 마커 방향(0/90/180/270°) 자동 판별
4. **부분 가림 강건**: 검출된 마커는 각자 독립적으로 ID 가짐

체커보드는 "위치 정확도"만, ArUco는 "식별 정확도"만 가짐 → **둘을 합친 게 Charuco**.

---

## 4. 옵션 A: EE로 Charuco 코너 터치 (선택한 방식)

### 4.1 핵심 아이디어

> "이 코너의 카메라 좌표"와 "이 코너의 로봇 좌표"를 같은 물리점에서 측정하여 묶는다.

```
보드 코너 ──┬─ EE 터치 (FK) ─────→ 매칭점_robot   ┐
            │                                     ├─ rigid 정렬 → 카메라→로봇 변환행렬
            └─ 카메라 + depth ───→ 매칭점_camera  ┘
```

EE가 **카메라 좌표계와 로봇 좌표계를 잇는 유일한 다리** 역할.

### 4.2 한 보드 위치에서 모든 코너를 다 터치할 필요 없음

- 이미지 1장으로 카메라는 보드의 모든 코너 픽셀과 3D를 자동 검출
- 그중 **닿기 좋은 4~5개**만 사람이 EE로 터치
- 보드를 다른 위치로 옮겨 반복 → 워크스페이스 전반에 점 분포

### 4.3 보드를 매번 옮길 때마다 다시 터치해야 함

```
보드 위치 A:  코너 ID 5 의 매칭점_camera = X1, 매칭점_robot = Y1
보드 위치 B:  코너 ID 5 의 매칭점_camera = X2, 매칭점_robot = Y2  ← 완전히 다른 점
```

같은 코너 ID라도 보드를 옮기면 세계 공간에서 **다른 물리점**. 캘리브에 필요한 건 ID가 아니라 매칭쌍 그 자체.

### 4.4 권장 수집 양

- 보드 위치 **2~3 곳** (가능하면 약간 기울여서 z 다양성 확보)
- 위치당 **5~7 코너** 터치
- **총 10~15 점** 이상

---

## 5. 픽셀 → 3D의 본질적 한계

### 5.1 단일 픽셀로는 3D 결정 불가

```
            카메라
              |
   (u,v) ──── ● ──── ? ──── ? ──── ?
              ↑
   픽셀 하나 = "이 광선 위 어딘가"라는 정보만 줌
```

어떤 캘리브든 (호모그래피, Charuco, 신경망) 이 한계는 동일.

### 5.2 광선을 점으로 줄이는 3가지 방법

| 방법 | 장점 | 단점 |
|---|---|---|
| **평면 가정 (z=table)** | depth 센서 불필요 | 비평면/공중 물체 불가 |
| **Depth 사용** | 진짜 3D, 시차 자동 보정 | depth 센서 필요 |
| **스테레오 삼각측량** | 단안에 비해 정확 | 두 시점 동기화 필요 |

**본 시스템에서는 D435 depth가 있으므로 "Charuco + Depth" 방식 사용.**

---

## 6. 처리 파이프라인

### [1] 픽셀 → 카메라 좌표계 3D 점

```
(u, v)
  │  ① cv2.undistortPoints(K, dist) — 렌즈 왜곡 제거
  ▼
정규화 좌표 (x_n, y_n, 1)         ← K⁻¹ · [u, v, 1]
  │  ② 단위 광선 방향(방향벡터)
  ▼
ray = (x_n, y_n, 1)
  │  ③ depth(u, v) [meters] 곱하기
  ▼
매칭점_camera = depth · (x_n, y_n, 1) = (X_c, Y_c, Z_c)
```

광선 방식(방향벡터)이라 **시차 자동 보정**: 픽셀이 어느 높이의 물체를 가리키든 (depth가 정확하면) 정확한 3D 좌표를 줌.

### Depth 방식의 한계

위 [1] 의 depth 기반 계산을 실제로 쓰면 다음 문제가 드러남:

- **D435 depth 정확도**: 70cm 거리에서 1~2% 오차 → **5~10mm 노이즈/픽셀**
- 5×5 패치 median 적용해도 픽셀당 5mm 수준 잔존
- → rigid 정렬 후 RMSE 5~10mm가 정확도 천장

**근본 원인**: 단일 픽셀의 3D 위치를 **그 픽셀의 depth 측정값** 하나에만 의존하기 때문. 보드 전체의 알려진 기하 정보를 못 씀.

---

## 7. 해결책: PnP (Perspective-n-Point) 기반 3D 좌표

### 핵심 차이: 단일 점 vs 여러 점 + 알려진 기하

**단일 픽셀 → 깊이 모름** (depth 방식이 이걸 측정으로 풀려고 함)

```
픽셀 (u, v) + K  →  3D 광선 방향만
                  →  광선 위 어디인지(depth) 모름

   [Camera] ──── ?  ─── ?  ─── ?
                ray
```

**두 픽셀 + 두 점 사이 거리를 알면 → 깊이 결정됨** ★

```
보드 코너 A, B는 정확히 38.8mm 떨어져 있다 (보드 사양으로 알려진 사실)
   픽셀_A = (200, 300)
   픽셀_B = (260, 300)

광선_A 위의 점 A와 광선_B 위의 점 B가 정확히 38.8mm 떨어져 있는 거리는
딱 하나만 존재 → 그게 깊이
```

```
        [Camera]
         /  \
        /    \
       /      \
      ●─38.8mm─●   ← 가까이 있으면 두 광선이 38.8mm 폭으로 좁다
     A         B

        [Camera]
         /     \
        /       \
       /         \
      /           \
     /             \
    ●─── 38.8mm ───●   ← 멀리 있으면 두 광선이 38.8mm 폭으로 넓다
   A               B
```

→ 카메라에서 **얼마나 떨어져야 광선들 사이가 정확히 38.8mm가 되는지** 는 단 하나의 거리에만 성립. 그게 깊이.

코너가 54개면 모든 쌍의 거리 제약이 동시에 충족돼야 → **6 DoF가 매우 정밀하게 결정**.

### PnP 알고리즘

```
입력:
  - N개 코너의 보드 좌표계 위치 (X_b, Y_b, 0)  ← 보드 사양으로 알려진 사실
  - N개 코너의 픽셀 위치 (u, v)                  ← 카메라가 본 것
  - K, dist                                     ← factory K

출력:
  - 보드 → 카메라 변환행렬 (R_bc, t_bc)
  - 부산물: 매칭점_camera_k = R_bc · 보드좌표_k + t_bc  (depth 안 씀!)
```

OpenCV: `cv2.solvePnP` 또는 `cv2.aruco.estimatePoseCharucoBoard`

### Depth 방식 vs PnP 방식

| 항목 | Depth 방식 | **PnP 방식** |
|---|---|---|
| 매칭점_camera 정확도 (70cm) | 5~10 mm | **0.5~1 mm** |
| 노이즈 원천 | depth 센서 | 픽셀 검출 (서브픽셀, ~0.05px) |
| Depth 카메라 필요 | ✅ | ❌ |
| 보드 기하 활용 | ❌ | ✅ |
| 보드 1프레임에서 풀이 | 코너별 따로 | **54개 동시** |
| 사용자 워크플로 | 동일 | 동일 (계산만 내부 변경) |

### 캘리브레이션 단계 (PnP 적용)

```python
for board_position in [위치1, 위치2, 위치3]:
    image = camera.capture()
    corners_uv, corners_id = charuco_detect(image)

    # ★ 한 프레임의 모든 검출 코너로 PnP 한 번 호출
    R_bc, t_bc = solvePnP_charuco(corners_uv, corners_id, board_spec, K, dist)

    for k in selected_corners:
        # 코너 ID로 보드 좌표계 위치 알아냄 (보드 격자 기하)
        pos_in_board = (col_k * 0.0388, row_k * 0.0388, 0)
        # PnP 결과로 카메라 좌표계 매칭점 계산 — depth 안 씀
        매칭점_camera_k = R_bc @ pos_in_board + t_bc

        EE.touch(physical_corner_k)
        매칭점_robot_k = robot.fk()
        pairs.append((매칭점_camera_k, 매칭점_robot_k))

# 이후 rigid 정렬은 동일
R, t = procrustes(매칭점_camera_list, 매칭점_robot_list)
```

### 실행 단계 (PnP 적용)

```
객체 검출이 픽셀(u,v)만 주는 경우 (스택 윗면 등):
  → PnP 못 씀 (보드 기하 없음) → depth 사용 필요

객체 검출이 알려진 기하의 패턴(예: ArUco 마커)인 경우:
  → PnP 사용 가능 → 정확도 mm 미만
```

캘리브 자체는 PnP로 mm 미만 달성 가능. 실행 시 일반 객체는 여전히 depth 또는 평면 가정 사용.

---

## 8. 한 줄 요약

> **Depth는 단일 픽셀당 1번 측정 → 5~10mm 노이즈가 천장.**
> **PnP는 N개 코너 + 알려진 보드 기하 → 노이즈 평균화 + depth 우회 → mm 미만.**
>
> 캘리브레이션의 본질은 동일하지만, 카메라 좌표계의 매칭점 계산을 **"depth 측정"** 에서
> **"보드 기하 + PnP 풀이"** 로 바꿔 정확도를 5~10배 향상시킨다.
