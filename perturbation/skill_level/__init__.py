"""Skill-level perturbation (planner ensemble).

Placeholder. Future: OMPL planner ensemble (RRT-Connect / PRM / BIT* / ...)
to vary the *shape* of trajectories that reach the same target, complementary
to subgoal-level offset which varies the *target itself*.
"""
